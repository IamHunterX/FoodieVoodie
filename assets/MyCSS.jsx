const imageStyle = {
    maxWidth: '100%',
    maxHeight: '100%',
  };
  const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      // alignItems: 'center',
      // justifyContent: 'center',
    },
    topView:{backgroundColor:'grey',
    flex:0.20,justifyContent:'center',
    alignItems:'center'}
  });

  export {styles}
  